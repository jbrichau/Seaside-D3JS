as yet unclassified
renderContentOn: html

html form: [
"...snipped code"
 html button
	bePush;
   onClick:  (html jQuery ajax
                  serializeForm;
                  json: [:json | json object: [ ] ];
                 onSuccess: 'console.log(arguments[0])');
   with: 'Filter!'
]